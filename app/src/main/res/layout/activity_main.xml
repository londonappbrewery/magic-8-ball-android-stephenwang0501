<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/activity_main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    android:paddingBottom="16dp"
    android:background="@color/back_ground_color"
    tools:context=".MainActivity">

    <RelativeLayout
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:text="@string/description"
            android:textSize="@dimen/ask_button_text"
            android:textStyle="bold" />

        <Button
            android:id="@+id/askButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/view_margin"
            android:layout_below="@id/image_eightBall"
            android:layout_centerHorizontal="true"
            android:text="@string/ask_button_text"
            android:textColor="@color/ask_button_text_color"
            android:textSize="@dimen/ask_button_text" />

        <ImageView
            android:id="@+id/image_eightBall"
            android:layout_width="@dimen/image_size"
            android:layout_height="@dimen/image_size"
            android:layout_below="@id/description"
            android:layout_marginTop="@dimen/view_margin"
            android:layout_centerHorizontal="true"
            android:background="@drawable/ball1"
            android:contentDescription="@string/ball_image_description" />
    </RelativeLayout>

</RelativeLayout>
